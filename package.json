{
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:e2e": "playwright test",
    "gen-sw": "node scripts/gen-sw.mjs",
    "gen-sw:check": "node scripts/gen-sw.mjs --check"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "http-server": "^14.1.1",
    "vitest": "^4.1.7"
  }
}
